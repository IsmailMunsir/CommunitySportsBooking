@{
    ViewData["Title"] = "Access Denied";
}

<section class="section container text-center">
    <div class="empty-state">
        <div class="icon">🔒</div>
        <h1>Sign in required</h1>
        <p>Please sign in as a member to access this page.</p>
        <a asp-controller="Account" asp-action="Login" class="btn btn-primary mt-16">Sign In</a>
    </div>
</section>